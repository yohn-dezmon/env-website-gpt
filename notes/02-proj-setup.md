# how to start up with Vite?

- install NodeJS
  - `node -v` shows you if it is already installed
    - `v22.8.0`

- create React app with Vite:
- `npm create vite@latest ny-climate-website --template react`

- install dependencies
- `npm install`

- start up the server 
- `npm run dev`

